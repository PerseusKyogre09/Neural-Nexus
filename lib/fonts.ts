// Define font variables without using next/font
// This avoids the SWC requirement that conflicts with Babel

export const fontSans = {
  variable: "--font-sans",
  className: "font-sans",
};

// Add Inter font via CSS in globals.css instead
// @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap'); 